// A bounded first milestone, not a replacement specification for the full queue.
export const queueCoreStageGoal = `Work only inside this workspace. Use Node.js ES modules, built-in libraries only, no package installation or external network. Preserve REQUIREMENTS.md. Finish without asking the user questions.
This assignment is only the first milestone of a larger queue project. Implement src/queue.js exporting JobQueue, package.json with type module and npm test, and node:test tests for this milestone.
JobQueue: constructor({maxAttempts=3}={}); enqueue(payload,{priority=0,key}={}) returns a job {id,payload,priority,key,status,attempts}. IDs are unique strings, status starts pending and attempts 0. Re-enqueuing a supplied key returns the existing job. dequeue() returns null when empty; otherwise picks highest priority pending job, FIFO for equal priorities, marks running and increments attempts. get(id) returns job or null. list({status}={}) preserves enqueue order and filters optionally. maxAttempts must be a positive integer; priority must be finite. Returned objects must not expose mutable internal state, including nested payload.
Acceptance: verify empty dequeue, priority and FIFO, dedupe, attempts/status transitions on dequeue, get/list/filter, invalid options, and nested payload isolation. Run npm test.
Complete/fail, snapshot/restore, persistence, CLI, and README are separate later milestones and are outside this assignment. Stop after implementing and testing this milestone; report what you actually verified.`;
