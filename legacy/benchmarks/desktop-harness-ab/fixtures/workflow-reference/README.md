This is a grader calibration fixture, never made available to evaluated agents. It implements the deterministic workflow contract so the independent verifier can be checked before any model run. It is not a model result. The queue supports caller-provided time, dependency readiness, retries, leases and snapshot recovery. The CLI and store adapters are intentionally small fixtures for test calibration.
