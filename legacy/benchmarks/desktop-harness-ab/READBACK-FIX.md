# 完成检查的读回证据修复

之前的最小文件任务已经成功读回，但完成检查的证据包只包含 test/build/lint/typecheck 命令，普通文件读取和 Shell 查看输出被遗漏。第一次审查因此要求再次验证；第二次仍没有结构化读回证据，却根据执行模型的最终答复放行。

## 修改

- 在证据包中单独提供最近最多 8 条真实文件读取和 Shell 输出，保留换行、退出状态、是否在最后一次修改之后，以及部分读取/截断标志；每条输出有长度上限。
- 文件工具补充原始文件字节数 `byte_size`。原有 `size` 是显示文本的字符数，且工具会规范化换行，不能将其当作原始字节数。
- 保留 test/build/lint/typecheck 的原有验证规则；读回成功不自动等于功能测试通过。
- 完成检查明确区分模型自述与宿主执行证据，并要求检查真实内容和证据限制。

证据包和 reviewer 的策略版本已更新，并进入 V3 配置指纹。旧检查点不被重写；新策略下不能直接恢复不同指纹的旧未完成任务。同版本新会话可正常继续。

## 真实最小任务对照

均通过桌面 `runDesktopNext`，主调用保持 GLM-5.3-Flash、max、128000，环境审计关闭，通用完成检查开启。要求写入并读回精确的 `PAW_TOOL_PROBE_OK\n`。

| 指标 | 修复前 | 修复后 |
| --- | ---: | ---: |
| 首次成功写入 | 9.822 秒 | 8.232 秒 |
| 总耗时 | 69.625 秒 | 23.352 秒 |
| 模型请求 | 7 | 4 |
| 工具调用 | 4 | 2 |
| 完成检查 | 2 次，首次要求重复验证 | 1 次，直接放行 |
| 已报告 token | 26760 | 14718 |
| 最终结果 | completed | completed |
| 独立字节验证 | 18 字节，完全匹配 | 18 字节，完全匹配 |

修复后的两次工具调用为一次写入和一次读取。原始响应中共 27 个工具片段，独立 Python 分析与 Paw 解析后的名称、ID、参数、顺序完全一致。三次流式主请求为 max/128000，辅助完成检查仍为既有的 high/4096 协议。

这是每条件一次的观察，不能将所有耗时差异都归因于代码修改，也不能外推复杂任务的完成率。

原始证据保存在本地忽略目录 `.runs/2026-09-07-readback-fixed/`，包括 `wire-analysis.json`、实际请求、原始响应、执行事件和文件字节验证。本轮未启用 Langfuse。
