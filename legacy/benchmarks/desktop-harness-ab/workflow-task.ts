export const workflowGoal = `Build a durable dependency-aware job scheduler with expiring worker leases. Work only in this workspace. Use Node.js 24 ES modules and built-in libraries, with no dependency installation or network. Preserve REQUIREMENTS.md. Implement src/queue.js exporting WorkflowQueue, src/store.js exporting async-compatible saveQueue(file, queue) and loadQueue(file), and src/cli.js. Add meaningful node:test tests, package.json with type=module and a working npm test, and README with API/CLI examples and recovery semantics. Run the tests and finish without asking questions. No daemon, UI or real-time sleeps are needed.

All times are caller-supplied nonnegative safe-integer milliseconds; never use Date.now inside the queue. All payload/result/error inputs must be JSON-safe (null, booleans, finite numbers, strings, arrays and plain objects recursively; reject undefined, cycles and non-finite numbers). Returned values, stored inputs and snapshots must not share mutable nested state with callers. Invalid operations must throw before changing queue state.

WorkflowQueue constructor({maxAttempts=3,retryDelayMs=10}={}): maxAttempts is a positive safe integer; retryDelayMs is a nonnegative safe integer.
add(id,payload,{dependsOn=[],priority=0,runAt=0}={}) returns a job. id is a nonempty string and unique; duplicate id throws. dependsOn is a duplicate-free array of IDs already present, excluding self. priority is a finite number. Dependencies are immutable. A dependency that is already failed/cancelled makes the new job immediately cancelled. Otherwise status=pending, attempts=0. Jobs expose at least id,payload,dependsOn,priority,runAt,status,attempts,leaseToken,leaseUntil,worker,result,error; inactive lease fields are null and initial result/error are null. list({status}={}) returns enqueue order, optionally filtered by one of pending/running/completed/failed/cancelled; get(id) returns job or null.
claim(worker,{now,leaseMs}) first validates worker as nonempty string, now and positive safe-integer leaseMs, and safe-integer deadline now+leaseMs. Then it calls expire(now). Choose a pending job with runAt<=now and every dependency completed, highest priority then FIFO. Return null if none. Mark running, increment attempts, set worker, leaseUntil=now+leaseMs, and a fresh nonempty string leaseToken. Tokens must never repeat within a queue, including after snapshot/restore and cancellation. Claiming increments attempts; expiry itself does not. Jobs in backoff or blocked on dependencies must not prevent other ready jobs from being claimed.
complete(id,token,result,now): require running, exact current token and now<leaseUntil, validate result, mark completed, store result and clear all lease fields. Return the resulting job.
fail(id,token,error,now): same lease/time checks, validate error. Store error and clear lease fields. If attempts<maxAttempts, status=pending and runAt=now+retryDelayMs*2**(attempts-1); otherwise status=failed. Return the resulting job.
expire(now): for every running job with leaseUntil<=now, perform the same retry/final-failure transition as fail, using error='lease expired' and supplied now for backoff. Return expired IDs in enqueue order. Lease deadline equality is expired. Late complete/fail must throw without implicitly expiring or otherwise changing any job; caller may explicitly expire or claim afterward.
cancel(id): require an existing pending or running job; mark cancelled, clear lease fields and return its job. A failed or cancelled job causes all its nonterminal descendants to become cancelled transitively, including descendants added later. Terminal completed/failed/cancelled jobs cannot be cancelled. Stale tokens never complete/fail cancelled or reclaimed jobs. All backoff/deadline arithmetic must remain a safe integer; reject an overflowing fail/expire/claim atomically (no partial changes anywhere).

snapshot() returns a JSON-serializable object whose top-level fields include version=1 and jobs (array in enqueue order), plus whatever config, ordering and token-counter state recovery needs. static WorkflowQueue.restore(snapshot) returns an independent queue, validates all state rather than trusting it, rejects unsupported version, invalid job field types/status/counts/times/lease invariants, duplicate IDs/tokens, missing dependency IDs, cycles and inconsistent retry counts (e.g. pending/running attempts greater than maxAttempts). Running leases stay running with the exact token/deadline after restore; they are reclaimed only by expire/claim at an expired time. Preserve config, ordering, results, errors, retries and future token uniqueness. snapshots are historical data, not executable code.

saveQueue atomically replaces the destination via a unique temp file in the same directory and rename, creates parent directories, and leaves no temp file after success. loadQueue returns a new default queue if missing and throws on malformed JSON or invalid snapshots. No write on a read-only query. Do not silently repair corrupt files.
CLI: node src/cli.js <file> <command> ...; add <id> <payloadJSON> [optionsJSON]; claim <worker> <now> <leaseMs>; complete <id> <token> <resultJSON> <now>; fail <id> <token> <errorJSON> <now>; cancel <id>; expire <now>; list [status]. Print exactly one JSON value on stdout for success (job, null, ID array, or job list as appropriate). Persist every mutation, including expiry done by a claim returning null. List does not create/change the file. Reject unknown commands, wrong argument counts, invalid numeric strings, malformed JSON and invalid queue operations with stderr and nonzero exit, leaving an existing store byte-for-byte unchanged. Parse CLI times as decimal nonnegative integers (no empty strings, fractions, exponents or trailing junk).

Explicit acceptance scenarios: dependency fan-in and transitive cancellation; priority/FIFO among eligible jobs; delayed jobs; exponential retry boundaries; lease expiry equality and stale-token rejection; restore while leases are active and subsequent reclamation; clone isolation; corrupt/cyclic snapshots; atomic overflow rejection; persistence and CLI across multiple processes. Keep the implementation focused. Use node --test (automatic discovery) or explicit test-file globs supported by Node 24; node --test test/ is not a valid directory entry point in this environment. Preserve test exit codes; run npm test directly for final verification. Do not change the task to make tests pass.`;

export const workflowBudget = Object.freeze({ wallMs: 1_200_000, calls: 64, reportedTokens: 160_000, maxSteps: 48 });
export const workflowImage = "paw-claude-bench:2.1.224";
