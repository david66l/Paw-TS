{
  "name": "paw",
  "private": true,
  "type": "module",
  "description": "Paw TypeScript stack (canonical). Lives under paw-ts/ only — do not import ../src/paw. Spec: 架构设计v2.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "bun run scripts/typecheck-all.ts",
    "lint": "biome check packages apps",
    "lint:fix": "biome check --write packages apps",
    "test:ts": "bun test packages/protocol packages/agent-loop packages/core packages/memory-core packages/memory-plugin packages/settings packages/workspace packages/harness packages/models packages/model-output-recovery packages/runtime packages/context-compaction packages/completion-review packages/output-recall packages/web-access packages/task-progress packages/collaboration packages/store packages/agent packages/progress-advisor",
    "test:desktop": "cd apps/desktop && bun test test",
    "test:memory": "bun test packages/memory/test",
    "desktop": "cd apps/desktop && bun run dev",
    "check:ts": "bun run lint && bun run typecheck && bun run test:ts && bun run test:desktop && bun run check:deps",
    "benchmark": "bun test benchmarks/",
    "benchmark:judge": "bun test benchmarks/judge/",
    "benchmark:swe": "cd benchmarks/swe-bench && python run.py",
    "benchmark:repo": "bun test benchmarks/repobench/",
    "benchmark:long": "bun test benchmarks/longbench/",
    "memory:migrate": "cd packages/memory && bun run migrate",
    "memory:migrate-legacy": "bun run packages/memory/src/runtime/migrate-legacy.ts",
    "memory:test:runtime": "cd packages/memory && bun run test:runtime",
    "memory:test:db": "cd packages/memory && bun run test:db",
    "memory:test:health": "bun test packages/memory/test/health.test.ts",
    "memory:check-cutover": "bash scripts/check-memory-cutover.sh",
    "check:deps": "bun run scripts/check-workspace-dependency-cycles.ts && bun test scripts/check-workspace-dependency-cycles.test.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.0",
    "bun-types": "^1.3.13",
    "playwright": "^1.62.1",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "tiktoken": "^1.0.22"
  }
}
